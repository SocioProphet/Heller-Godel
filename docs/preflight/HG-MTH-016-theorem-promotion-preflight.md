# HG-MTH-016 Theorem-Promotion Preflight

Issue: `#143`  
Target identifier: `HG-MTH-016`  
Target object: P3.b canonical statistic and generating-function at `p=3` closure  
Preflight base: `8cddffc630762823657026878d72a21d23901b36`  
Preflight status: scope assessment only  
Promotion status: not authorized by this document

## 1. Existing document content and current claim level

The current `HG-MTH-016` document is:

```text
docs/gate-minimality/p3b-generating-function-p3-closure.md
```

It currently declares:

```text
Claim level: method-grade modulo candidate-HG-FND-002.
```

The document proves the P3.b generating-function closure for the arity-three constructor grammar:

```text
C_A^(3) = (A -> A -> A -> A) -> A -> A
T ::= x | t(T,T,T)
```

under constructor-shape statistic. It records the functional equation:

```text
C_3(x)=1+x C_3(x)^3,
C_3(0)=1,
```

and the Fuss-Catalan coefficient extraction:

```text
[x^n]C_3(x)=FC_n^(3)=1/(2n+1) * binom(3n,n).
```

No new identifier is required. Promotion, if later authorized, should be an in-place grade promotion of `HG-MTH-016`.

## 2. Proof components

The promotion theorem is assembled from the existing P3.b closure document plus normalized `HG-FND-002`.

| Component | Source object | Verification surface |
| --- | --- | --- |
| Constructor-shape statistic and grammar dependency discipline | `HG-FND-001`, `HG-FND-002` | `HG-MTH-016` Sections 1-3 |
| Functional equation `C_3(x)=1+xC_3(x)^3` | `HG-FND-002` | structural recursion in `HG-MTH-016` Section 2 |
| Fuss-Catalan coefficient extraction `FC_n^(3)=1/(2n+1)binom(3n,n)` | `HG-FND-002` | Lagrange inversion calculation in `HG-MTH-016` Section 2 |
| Reduced/raw product-statistic discipline | `HG-FND-002` | `tests/test_hg_fnd_002_generating_function.py` product-formula tests |
| Analyticity at origin and selected local branch `C_3(0)=1` | `HG-FND-002` | implicit-function argument in `HG-MTH-016` Section 5 |

The existing product-formula tests are substantive for reduced/raw statistic discipline:

```text
test_chain_product_formula_under_reduced_statistic
test_catalan_product_formula_under_reduced_statistic
```

The same test file also contains two tautological singularity-record tests. Those do not block `HG-MTH-016` promotion because the promotion target is the P3.b generating-function closure, not P3.c Puiseux singularity location or decorated singular datum.

Promotion theorem candidate:

```text
HG-MTH-016 Theorem. Under the constructor-shape statistic on alpha-classes of eta-long beta-normal de Bruijn lambda terms generated by T ::= x | t(T,T,T), the P3.b proof-class generating function is the unique local analytic branch at x=0 with C_3(0)=1 satisfying

C_3(x)=1+xC_3(x)^3.

Its coefficients are the arity-three Fuss-Catalan numbers

[x^n]C_3(x)=1/(2n+1) * binom(3n,n).

This theorem is scoped to the P3.b generating-function construction and coefficient extraction. It does not locate or decorate the dominant singularity; that belongs to HG-MTH-018 / HG-FND-003.
```

## 3. Dependency check

The dependency ceiling has been discharged in the framework registry:

| Dependency | Current registry status | Recorded normalization SHA |
| --- | --- | --- |
| `HG-FND-002` | normalized Tier 1 | `d4b6db26b071d28d8c5597d1107e1663b0b773cf` |

`docs/framework-foundations/HG-FND-002-proof-class-generating-function.md` records the normalized proof-family generating-function construction, including target witness family, declared statistic, reduced/raw statistic distinction, product formula discipline, and singularity-extraction boundary.

`docs/framework-core/claim-grammar.md` already records that the `HG-MTH-016` candidate ceiling has been discharged while keeping `HG-MTH-016` method-grade.

Therefore the promotion PR, if later authorized, would not need to normalize `HG-FND-002`; it would only update the grade of `HG-MTH-016` and register the corresponding theorem-grade boundary.

## 4. Anti-seed requirement

The promotion PR should add a generating-function-specific anti-seed if no existing anti-seed is sufficient for theorem-grade citation of `HG-MTH-016`.

Recommended new anti-seed:

```text
A-HG-MTH-005 — Citing HG-MTH-016 theorem-grade without declared statistic and branch
```

Failure mode:

```text
A downstream artifact cites HG-MTH-016 as theorem-grade generating-function data without declaring the constructor-shape statistic and selected local branch C_3(0)=1, or treats the P3.b closure as a decorated Puiseux singular datum.
```

Correct boundary:

```text
HG-MTH-016 theorem-grade citation licenses only the constructor-shape P3.b generating-function equation, coefficient extraction, and selected local analytic branch at the origin. Dominant singularity decoration, Puiseux coefficients, monodromy, and chi_3 source identification belong to HG-MTH-018 / HG-FND-003 / HG-VOC-006.
```

`A-HG-MTH-004` is already used by `HG-MTH-018` for orientation convention. The next available `A-HG-MTH-*` identifier is therefore `A-HG-MTH-005`, unless the anti-seed register changes before the promotion PR.

## 5. Distance-classification placement

`HG-MTH-016` should move into Tier 1 only as a theorem-grade MTH object, not as an `HG-FND-*` surface.

The promotion PR should add a row under the existing Tier 1 theorem-grade MTH objects subsection:

```text
| `HG-MTH-016` | P3.b canonical statistic and generating-function at p=3 closure | theorem-grade; depends on HG-FND-002 @ `d4b6db2`; governed by <anti-seed-id>; source: `docs/gate-minimality/p3b-generating-function-p3-closure.md` |
```

This preserves the distinction between normalized foundation surface `HG-FND-002` and the P3.b theorem-grade MTH closure using that surface.

## 6. Claim-grammar updates required

A later promotion PR must update `docs/framework-core/claim-grammar.md` in three places:

1. Bootstrap active identifiers table:

```text
HG-MTH-016 ... theorem-grade within declared P3.b constructor-shape generating-function scope; depends on normalized HG-FND-002; statistic and branch convention required
```

2. The `HG-MTH-016` non-claims block:

Preserve these boundaries:

- no `HG-MTH-011` promotion;
- no `HG-MTH-018` promotion or restatement;
- no `HG-MTH-020` promotion;
- no `HG-MTH-021` promotion;
- no P3.c closure or Puiseux decoration claim;
- no P3.d closure;
- no `zeta_3` / `U(2)` correspondence;
- no `A_n` theorem;
- no Clay claim;
- no Heller-Einstein authorization;
- no downstream repo work.

3. Example downstream citation block:

Update the `HG-MTH-016` row to theorem-grade only within the declared local theorem scope:

```text
[HG-MTH-016 @ <sha>] — P3.b canonical statistic and generating-function at p=3 closure; citation grade: theorem-grade within declared constructor-shape generating-function scope; licensed use: grammar T ::= x | t(T,T,T), constructor-shape statistic, C_3(x)=1+xC_3(x)^3, C_3(0)=1, and Fuss-Catalan coefficient extraction only.
```

## 7. Non-promotion checklist

The promotion PR must explicitly state that it does not:

1. promote `HG-MTH-011`;
2. promote `HG-MTH-018` or change its theorem;
3. promote `HG-MTH-020`;
4. promote `HG-MTH-021`;
5. close P3.c;
6. close P3.d;
7. prove or extend to an `A_n` family;
8. authorize Heller-Einstein work;
9. cross into any Clay-program proof claim;
10. perform downstream repo work;
11. treat the P3.b generating function as decorated Puiseux datum.

## 8. Promotion PR file set if authorized later

If this preflight merges and the promotion is subsequently authorized, the promotion PR should modify exactly four files:

1. `docs/gate-minimality/p3b-generating-function-p3-closure.md` — add formal theorem statement and proof block citing normalized `HG-FND-002` SHA.
2. `docs/framework-core/claim-grammar.md` — update `HG-MTH-016` grade, non-claims, and example citation block.
3. `docs/framework-core/distance-classification.md` — add `HG-MTH-016` under the Tier 1 theorem-grade MTH subsection.
4. `docs/framework-core/anti-seed-framework.md` — add the statistic/branch/Puiseux-boundary anti-seed under the next available non-conflicting `A-HG-MTH-*` identifier.

No new tests are required for the promotion PR unless review demands a guard for the Lagrange-inversion algebra. The existing `HG-FND-002` product-formula tests cover reduced/raw statistic discipline but do not mechanically prove the full Lagrange inversion derivation.

## 9. Preflight conclusion

`HG-MTH-016` is a plausible theorem-grade promotion candidate after `HG-MTH-018`, but its promotion should be narrower than `HG-MTH-018`.

It should license only the P3.b constructor-shape generating-function equation, selected local branch at the origin, and Fuss-Catalan coefficient extraction. It should not license dominant-singularity location, Puiseux decoration, monodromy, `chi_3`, `zeta_3`, or `U(2)` claims.

This preflight does not authorize the promotion PR. It records the exact promotion boundary and the required file set for later authorization.
