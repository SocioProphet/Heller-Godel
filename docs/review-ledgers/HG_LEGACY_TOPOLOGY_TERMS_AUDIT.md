# HG Legacy Topology Terms Audit

Status: corrected authoritative audit ledger.  
Authoritative result date: 2026-05-14.  
Scanner: `scripts/audit_legacy_topology_terms.py`.  
Authoritative run: GitHub Actions `legacy-topology-audit` against a local clone after PR #29.  
Result: **No scoped hits found.**

## Purpose

This ledger records the current scoped topology-terminology audit state while preserving the historical connector-backed observations that motivated the audit.

The governing discipline is:

```text
audit first -> freeze findings -> run authoritative local scanner -> correct ledger -> patch only if scoped hits exist
```

The authoritative local scanner found no scoped hits, so no theorem-core remediation is currently required.

## Authoritative scanner result

The exact scanner payload used for future `--diff-against-frozen` comparisons is marked below. Historical context is intentionally outside the markers and is not part of the machine comparison.

<!-- AUDIT_REPORT_START -->
# Legacy Topology Terminology Audit

Generated by `scripts/audit_legacy_topology_terms.py`.

## Scope classes

- **theorem-core** — fail on hit: `yes` — Active theorem/proof/computation surface. Hits require remediation or an inline exemption. Patterns: `docs/manuscripts/paper_i_*.md`, `docs/appendices/appendix_a_*.md`, `docs/gates/minimality.md`, `src/heller_godel/*.py`
- **context-no-patch** — fail on hit: `no` — Context and source evidence. Hits are reported but should not be rewritten as theorem-core remediation. Patterns: `docs/context/**/*`, `sources/primary/**/*`, `sources/context/**/*`, `data/manifests/**/*`
- **historical-no-patch** — fail on hit: `no` — Historical/source-capture material. Preserve unless a separate archival normalization is approved. Patterns: `docs/source-captures/**/*`, `**/*historical*`, `**/*history*`
- **legacy-manuscript-no-patch** — fail on hit: `no` — Superseded or legacy manuscript material. Report separately from active Paper I theorem core. Patterns: `docs/manuscripts/calculus_invariant_characters_*.md`, `docs/manuscripts/*patch_plan*.md`

## Pattern coverage

- **pi-one-base-symbol** / `core-blocker` — Use current finite-local-system pi_1 language, or use \\mathsf{Path}(B) only in path-category transport sections.
- **fundamental-path-groupoid** / `core-blocker` — Use geometric path category where path transport, not homotopy-class groupoid structure, is intended.
- **generic-groupoid-language** / `review-required` — Review in theorem-core. Groupoid language is allowed only when an actual groupoid object is being defined, not as stale path-space shorthand.
- **homotopy-class-language** / `core-blocker` — Use piecewise-smooth paths modulo orientation-preserving reparametrization where that is the intended object.
- **old-fiber-product-path-object** / `core-blocker` — Use E ×_{B,s} \\mathsf{Path}(B) only in horizontal-lift/path-category sections.
- **old-horizontal-lift-domain** / `core-blocker` — Use γ ∈ \\mathsf{Path}(B) and type Φ_γ : E_{s(γ)} → E_{t(γ)} only if horizontal lifts are active.
- **simply-connected-proof-rationale** / `review-required` — Allowed only as sanity-check topology; do not use it as a theorem-grade substitute for the current finite-local-system argument.
- **every-loop-contractible-rationale** / `review-required` — Allowed only in historical/context notes; theorem-core should use explicit pi_1/local-system triviality if applicable.

## Hits

No scoped hits found.
<!-- AUDIT_REPORT_END -->

## Interpretation

The local-clone scanner is the ground truth for the scoped policy. As of this audit:

```text
--fail-on-core:         PASS
--diff-against-frozen:  expected to PASS after this correction
scoped hits:            none
remediation required:   none
```

This means the prior remediation need was overstated by the connector-backed pre-scan. The current theorem-core does not contain forbidden scoped topology/path terminology under the committed scanner.

## Historical context: connector-backed pre-local-run observations

The observations below are retained to preserve the audit trail. They were produced before an authoritative local clone run was available and are superseded for remediation purposes by the marked scanner result above.

### Active Paper I as theorem surface

The README identified the active capture as:

```text
docs/manuscripts/paper_i_deligne_cohomological_phase_characters.md
```

It also described the current defensible core in terms of proof-class generating functions, decorated Puiseux singular data, branch-killing cyclic covers, Deligne units, finite monodromy/deck characters, lifted phase indices, and section-defect carry cocycles.

This established that the old v1.6 bundle/path-category replacement plan should not be blindly applied across the repository.

### Connector-backed pattern observations

The connector-backed pre-scan reported possible occurrences in areas such as:

```text
docs/gates/minimality.md
docs/appendices/appendix_a_chain_catalan_witness.md
docs/context/proof-dynamics-mu2-bridge-artifact.md
docs/manuscripts/paper_i_deligne_cohomological_phase_characters.md
docs/manuscripts/calculus_invariant_characters_v2_1_3.md
docs/source-captures/calculus_invariant_characters_capture.md
docs/manuscripts/calculus_invariant_characters_v2_1_3_patch_plan.md
```

The authoritative scanner supersedes this as follows:

- `pi_1` notation in finite monodromy or holonomy character domains is permitted and not a stale `Π₁(B)` path-category issue.
- `S^2` may be used as a trivial finite-local-system sanity check via `pi_1(S^2)=0`; it is forbidden only as a theorem-grade substitute for a path/holonomy construction.
- `groupoid` language is now in scanner scope. No scoped hits were found in the authoritative run.
- Context, source-capture, historical, and legacy-manuscript observations are not theorem-core remediation targets unless a separate archival normalization task is opened.

## Formal separation: carry versus Deligne symbol

The audit continues to preserve the separation:

```text
finite arithmetic carry / section defect != Deligne cup-product regulator symbol
```

Required invariant:

- The carry term belongs to the finite lifted-index arithmetic layer. In the current implementation, it is the section-defect cocycle for `0 -> Z -> Z -> Z/L -> 0`.
- The Deligne cup-product symbol belongs to the regulator-symbol layer and lands in a degree-2 Deligne-cohomology target.
- These objects have different degrees, functoriality, and computational status.

The code should continue to tag these separately. In `phase_characters.py`, functions such as `carry`, `carry_table`, and `carry_cocycle_identity_holds` are finite-arithmetic helpers. The helper `tame_symbol_standard` records the regulator-symbol boundary convention and must not be described as computing carry.

## Scope maintenance note

The scanner's scope should be reviewed when the theorem stack grows. In particular, new active manuscript/proof paths must either match the existing glob patterns or be added explicitly to `FILE_CLASSES` in `scripts/audit_legacy_topology_terms.py`.

Current theorem-core paths are:

```text
docs/manuscripts/paper_i_*.md
docs/appendices/appendix_a_*.md
docs/gates/minimality.md
src/heller_godel/*.py
```

If a new canonical manuscript, proof appendix, gate specification, or source package is added outside those paths, the audit gate must be updated in the same PR or in a preceding infrastructure PR.

## Decision log

- The stale v1.6 replacement bundle was not applied globally.
- The connector-backed pre-scan was treated as historical context only after local CI output became available.
- The canonical active manuscript is Paper I, not the older v2.1.3 floor-function-first manuscript.
- `pi_1` notation is allowed in finite-local-system theorem-core where it denotes finite monodromy/holonomy character domains.
- `\\mathsf{Path}(B)` should only be introduced where actual geometric path-category transport is being defined.
- `S^2` may be used as a trivial sanity-check base via `pi_1(S^2)=0`, but not as a theorem-grade substitute for a path/holonomy construction.
- Generic `groupoid` is explicitly in audit scope as review-required terminology.
- No theorem-core remediation is currently required because no scoped hits were found.
