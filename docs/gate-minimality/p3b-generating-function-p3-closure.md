# HG-MTH-016 — P3.b Generating Function at p=3 Closure

Identifier: `HG-MTH-016`  
Status: closure document for P3.b; scoped by `HG-MTH-015`.  
Owner: `SocioProphet/Heller-Godel`.  
Track: P3.b under `HG-MTH-012`, after P3.a closure `HG-MTH-014`.  
Claim level: method-grade modulo candidate-`HG-FND-002`.

## 1. Statement of closure

P3.b closure verifies the four S-requirements from `HG-MTH-015` against the arity-three Fuss-Catalan generating function:

```text
C_3(x)=1+x C_3(x)^3
```

using the constructor-shape statistic on the grammar constructed in `HG-MTH-014`:

```text
C_A^(3) = (A -> A -> A -> A) -> A -> A
T ::= x | t(T,T,T)
```

Closure statement:

Under the constructor-shape statistic on alpha-classes of eta-long beta-normal de Bruijn lambda terms of type `C_A^(3)`, the proof-class / proof-family generating function is:

```text
C_3(x)=sum_{n >= 0} FC_n^(3) x^n
```

where:

```text
FC_n^(3)=1/(2n+1) * binom(3n,n).
```

This generating function satisfies:

```text
C_3(x)=1+x C_3(x)^3,
C_3(0)=1.
```

It is analytic at `x=0` and has a finite positive radius of convergence. The precise location of the dominant singularity, recorded in `HG-MTH-014` as the traceability value `rho_3=4/27`, remains a P3.c-grade obligation. This PR does not close P3.c.

## 2. Verification strategy

This closure selects the combined V2 + V3 strategy from `HG-MTH-015`.

V2 establishes the functional equation structurally from the grammar:

```text
T ::= x | t(T,T,T).
```

The base normal form `x` contributes the constant term `1` under the constructor-shape statistic because it has zero `t`-nodes. Any non-base normal form is uniquely of the form:

```text
t(T1,T2,T3)
```

where `T1`, `T2`, and `T3` are normal forms generated by the same grammar. The top constructor contributes one `t`-node, and the three subterms contribute independently. Therefore the ordinary generating function obeys:

```text
C_3(x)=1+x C_3(x)^3.
```

V3 extracts the coefficients from this algebraic equation by Lagrange inversion. Let:

```text
Y(x)=C_3(x)-1.
```

Then:

```text
Y=x(1+Y)^3.
```

For `n >= 1`, Lagrange inversion gives:

```text
[x^n]Y = 1/n [u^(n-1)](1+u)^(3n)
       = 1/n * binom(3n,n-1)
       = 1/(2n+1) * binom(3n,n).
```

Since `C_3=1+Y`, the constant coefficient is `FC_0^(3)=1`, and for all `n >= 1`:

```text
[x^n]C_3(x)=FC_n^(3)=1/(2n+1) * binom(3n,n).
```

V1 is used only as a sanity check, not as the primary closure mechanism:

```text
FC_0^(3)=1,
FC_1^(3)=1,
FC_2^(3)=3,
FC_3^(3)=12,
FC_4^(3)=55,
FC_5^(3)=273.
```

The V2 structural recursion supplies the generating-function equation. The V3 coefficient extraction supplies the general Fuss-Catalan identity.

## 3. S1 — Statistic well-definedness

The constructor-shape statistic is:

```text
k_t(T)=number of t-nodes in T.
```

It is well-defined on alpha-classes of eta-long beta-normal de Bruijn lambda terms of type `C_A^(3)`.

Alpha-equivalence respect: de Bruijn representation removes dependence on variable names. Renaming bound variables changes no constructor nodes. Therefore `k_t` is invariant under alpha-equivalence.

Finiteness: every normal form generated by the grammar is a finite expression built by finitely many applications of `t` to subterms, with base case `x`. Therefore `k_t(T)` is a finite nonnegative integer for each normal form.

Compatibility with the underlying canonical statistic: the manuscript records:

```text
sigma_C(s)=#lambda-nodes + #application-nodes + #variable-leaves.
```

The constructor-shape statistic used in `C_3(x)` is a projection of `sigma_C`, not raw `sigma_C` on the underlying lambda term on the nose. If:

```text
sigma_C = L + A + V
```

where `L` is lambda-node count, `A` is application-node count, and `V` is variable-leaf count, then on the P3.a grammar the statistic `k_t` records the arity-three constructor applications, i.e. the `t`-node part of the application structure. This matches the A1 paradigm, where the Catalan series enumerates binary constructor applications in:

```text
T ::= x | b(T,T)
```

rather than the raw lambda-node plus application-node plus variable-leaf total of the underlying eta-long beta-normal lambda term.

Thus S1 is verified at method-grade modulo candidate-`HG-FND-002`.

## 4. S2 — Formal-series enumeration

P3.b closure verifies:

```text
C_3(x)=sum_{n >= 0} a_n x^n
```

where `a_n` is the number of alpha-classes of eta-long beta-normal terms of type `C_A^(3)` with exactly `n` ternary constructor nodes.

By V2, the grammar gives:

```text
C_3(x)=1+x C_3(x)^3.
```

By V3, applying Lagrange inversion to:

```text
Y=x(1+Y)^3,
C_3=1+Y,
```

gives:

```text
a_n=FC_n^(3)=1/(2n+1) * binom(3n,n).
```

The first coefficients are:

```text
C_3(x)=1+x+3x^2+12x^3+55x^4+273x^5+...
```

These are the arity-three Fuss-Catalan coefficients under constructor-shape enumeration. Therefore S2 is verified.

## 5. S3 — Analyticity at origin

Define:

```text
F(x,y)=y-1-x y^3.
```

At `(x,y)=(0,1)`:

```text
F(0,1)=0,
partial F / partial y = 1 - 3x y^2,
(partial F / partial y)(0,1)=1.
```

The analytic implicit function theorem therefore gives a unique analytic function `C_3(x)` in a neighborhood of `x=0` such that:

```text
F(x,C_3(x))=0,
C_3(0)=1.
```

Equivalently:

```text
C_3(x)=1+x C_3(x)^3,
C_3(0)=1.
```

The coefficients extracted above are finite nonnegative integers, and the algebraic equation defines a nonzero radius of convergence for the selected analytic branch. S3 is verified.

This PR does not locate the radius at P3.c-grade. The traceability value `rho_3=4/27` remains downstream to P3.c.

## 6. S4 — Continuation discipline

`C_3(x)` admits algebraic continuation as a branch of the cubic algebraic curve:

```text
y=1+x y^3.
```

Equivalently:

```text
x y^3 - y + 1 = 0.
```

The P3.b closure uses the branch selected near the origin by:

```text
C_3(0)=1.
```

This satisfies the continuation discipline required at the `HG-FND-002` level: the generating function is not merely a finite enumeration table; it is specified as a formal power series at the origin and as a local analytic branch of an algebraic equation.

The full analytic-continuation structure of this branch, including branch points, Puiseux expansion, monodromy, and finite phase reduction, is not part of P3.b. Those belong to P3.c and P3.d.

S4 is verified at the scope appropriate to P3.b.

## 7. Grade declarations

| Object | Grade | Source |
| --- | --- | --- |
| `HG-FND-002` | candidate; registry normalization pending | `docs/framework-core/distance-classification.md` |
| `HG-MTH-015` (P3.b scope) | method-grade as scope | PR #84 |
| `HG-MTH-016` (P3.b closure, this PR) | method-grade modulo candidate-`HG-FND-002` | this PR |
| `HG-MTH-014` (P3.a closure) | method-grade modulo candidate-`HG-FND-001` | PR #82 |

If `HG-FND-002` normalization closes in a future PR, this P3.b closure can be re-graded against the normalized surface without revisiting the constructed generating function.

The two modulo-candidate dependencies currently active in the P3 chain are independent:

```text
P3.a closure: modulo candidate-HG-FND-001
P3.b closure: modulo candidate-HG-FND-002
```

Normalizing one does not automatically normalize the other. Full theorem-track promotion of `HG-MTH-011` remains blocked until all downstream P3 candidate surfaces required by P3.a/P3.b/P3.c/P3.d are normalized or otherwise discharged.

## 8. Heller-Einstein mediation status

This is pure Heller-Godel closure.

No Heller-Einstein `HE-INT-*` typed-morphism route is used. No Heller-Einstein `HE-PLC-*` placeholder route is used. The generating-function construction closes directly inside the Heller-Godel proof-family framework at method-grade modulo candidate-`HG-FND-002`.

## 9. Non-claims

1. Does not promote `HG-FND-002` from candidate to settled Tier 1.
2. Does not promote `HG-FND-001` from candidate to settled Tier 1.
3. Does not close P3.c. The location `rho_3=4/27` remains traceability from `HG-MTH-014` and is not P3.c-closure-verified by this PR.
4. Does not close P3.d. The `zeta_3` / `U(2)` correspondence remains a downstream hook.
5. Does not promote `HG-MTH-011` from method-grade-as-candidate.
6. Does not extend to `A_n` for `n >= 3` beyond A2.
7. Does not authorize Heller-Einstein PRs.
8. Does not cross into `SocioProphet/yang-mills`.
9. Does not rely on numerical verification beyond the small-`n` sanity check listed in Section 2.

## 10. Future closure pathway

After this closure merges, P3.c scope is unlocked: Puiseux singularity and `chi_3` specification at `p=3`, attaching to `HG-FND-003` and `HG-VOC-006`.

P3.c scope is separately authorized. This document does not open P3.c.

## 11. Identifier and registry

This document assigns:

```text
HG-MTH-016
```

to the P3.b canonical statistic and generating-function at `p=3` closure.

`docs/framework-core/claim-grammar.md` is the canonical identifier registry. This PR updates that registry to register `HG-MTH-016` and record its grade ceiling as method-grade modulo candidate-`HG-FND-002`.
