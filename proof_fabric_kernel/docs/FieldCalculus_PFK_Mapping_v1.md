# Field Calculus / PFK Mapping v1

PFK maps domain activity into a common operational stack:

```text
domain event -> Event-IR -> validation -> ProofArtifact -> claim ledger
```

The same proof-fabric contract can host prime-statistics experiments, lawful-learning replay, and abstract governance examples when every artifact carries provenance and a claim boundary.
